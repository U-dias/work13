<h1>Rooms#photo_upload</h1>
<p>Find me in app/views/rooms/photo_upload.html.erb</p>

<div class="container mt-4">
    <div class="row">
        <div class="col-md-3">
            <%= render 'room_menu' %>
        </div>
        <div class="col-md-9">
            <div class="card">
                <div class="card-body">
                <h4 class="mt-4 mb-4"><b>写真アップロード</b></h4>
                <!-- 写真アップロード -->
                <%= image_tag user_url(current_user), :size => '200x200', class: "img-fluid img-thumbnail rounded-pill" %>
                <h4 style="margin-left: 5.5rem;"></h4>

                <div class="collapse" id="collapse1">
                <div class="card card-body">
                    <%= form_for :room, url: rooms_url(current_user), action: :update, method: :post do |f| %>
                        <%= f.file_field :photos, class: "input-group-text", onchange: "this.form.submit();" %>
                    <% end %>   
                </div>


                 </div>
                 </div>
                </div>      
                </div>
            </div>
        </div>
    </div>
</div>
# ユーザーアイコンのアップ
<%= form_with(model: @user) do |form| %>
    <div class="field">
      <%= form.label :profile_image %>
      <%= form.file_field :profile_image %>
    </div>
  
    <%= form.submit %>
<% end %>

<script>
function check(){
        if(window.confirm('本当に削除してよろしいですか？')){ 
          return true;}
        else{window.alert('キャンセルされました'); 
          return false; }}    
</script>
